import { StyleSheet } from "react-native";
import { SIZES } from "../../constants/sizes";

export const searchSuggestions = StyleSheet.create({
  container: {
    display: "flex",
    rowGap: 5,
  },
});
